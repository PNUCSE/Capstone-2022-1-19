export interface ReadDashResponse {
  // flavor_id: string;
  // ram_size: number;
  // volume_size: number;

  num_instances: number;
  total_disk_size: number;
  total_num_cpu: number;
  total_ram_size: number;
  // gpu_size: number;
}
